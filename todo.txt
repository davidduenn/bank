make a server which listens to a port and clients which connect to the server for transactions.

fix unit test after member code introduced

implement rm_member

implement rm_account

Merge in the old bank.cpp file

only set member id if it doesn't already exist

move 'next' account pointer to private

Add in connections for a DB

bank::add_account return bool?
